import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#191919',
    padding: 24,
  },
  title: { marginTop: 48, color: '#FBFCFE', fontSize: 24, fontWeight: 'bold' },
});
